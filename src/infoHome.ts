const infoHome = (name: string, age: number) => {
    console.log(`Hello ${name}, you are ${age} years old`);
    return `<h2><strong>Hello ${name}, you are ${age} years old</strong></h2>`;
}

export default infoHome;